VITE_API_BASE_URL=http://localhost:8000

# Supabase project URL + anon/public key (safe to expose client-side —
# Supabase's security model is enforced by RLS policies, not by keeping
# this key secret). Used directly by the frontend for signup/login.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
